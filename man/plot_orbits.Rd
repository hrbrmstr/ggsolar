% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-orbits.R
\name{plot_orbits}
\alias{plot_orbits}
\title{Return a skeleton of a ggplot2 plot of solar system plot}
\usage{
plot_orbits(
  orbits,
  planet_positions,
  label_planets = FALSE,
  orbit_fill = NA,
  orbit_color = "white",
  orbit_linewidth = 0.125/2,
  planet_size = 3,
  planet_shape = 21,
  planet_stroke = 0.5,
  planet_color = "#fea100",
  planet_fill = scales::alpha("#fea100", 1/4),
  label_color = "white",
  label_size = 3,
  label_family = "sans",
  label_hjust = 0
)
}
\arguments{
\item{orbits}{(df)}

\item{planet_positions}{(df)}

\item{label_planets}{(bool) if \code{TRUE}, then planet labels will be
naively placed to the right of each planet point.}

\item{orbit_fill, orbit_color, orbit_linewidth}{defaults}

\item{planet_size, planet_shape, planet_stroke, planet_color, planet_fill}{defaults}

\item{label_color, label_size, label_family, label_hjust}{defaults (if \code{label_planets} is \code{TRUE})}
}
\value{
ggplot2 object
}
\description{
THIS JUST A LAZY HELPER FUNCTION UNTIL I HAVE TIME TO WRITE GEOMS
}
\details{
If you need to be able to vary planet size/color/fill/etc by the
planet name, or you want different orbit aesthetics mapped by planet,
you'll need, for now, to run \code{\link[=scaffold_planet_plot]{scaffold_planet_plot()}} which will place
a textual copy of this function to stdout that you can copy and
use at-will.

Given a data frame built with \code{\link[=generate_orbits]{generate_orbits()}} and another
data frame of planet positions, theoretically generated by
\code{\link[=randomize_planet_positions]{randomize_planet_positions()}}, return a ggplot2 skeleton with —
at a minimum — a polygon geom and point geom. A text geom can
optionally be added, but only naive positioning of labels is performed.

\if{html}{
  \out{<div style="text-align: center">}\figure{README-sol-1.png}{options: style="width:750px;max-width:75\%;"}\out{</div>}
}
\if{latex}{
  \out{\begin{center}}\figure{README-sol-1.png}\out{\end{center}}
}
}
\examples{
sol_orbits <- generate_orbits(sol_planets)

set.seed(1323)

placed_planets <- randomize_planet_positions(sol_orbits)

plot_orbits(
  orbits = sol_orbits,
  planet_positions = placed_planets,
  label_planets = TRUE,
  label_family = hrbrthemes::font_es_bold
) +
  hrbrthemes::theme_ipsum_es(grid="") +
  coord_equal() +
  labs(
    title = "Sol",
    caption = "Pluto is 100\% a planet"
  ) +
  theme_enhance_solar()
}
